
  # Build GroSave Mobile App

  This is a code bundle for Build GroSave Mobile App. The original project is available at https://www.figma.com/design/SVhYCHEUhnC0bCbmnfW3VS/Build-GroSave-Mobile-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # GroSave
